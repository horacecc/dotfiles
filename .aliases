#!/usr/bin/env zsh

alias g='git'

alias claude-docker='docker run -it --rm \
  -v "$(pwd):/workspace" \
  -v claude-auth:/claude-config \
  claude-docker'

alias codex-docker='docker run -it --rm \
  -v "$(pwd):/workspace" \
  -v codex-auth:/codex-config \
  codex-docker'
