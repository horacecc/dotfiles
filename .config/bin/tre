#!/usr/bin/env bash
#compdef tree

# With arguments: acts like `tre .` or `tren .`

emulate -L zsh

unalias tre 2> /dev/null
unalias tren 2> /dev/null

function tre() {
	tree -aC -I '.git|node_modules|bower_components' --dirsfirst "$@"
}

function tren() {
	tre | less -FRNX
}
